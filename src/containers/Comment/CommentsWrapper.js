import React, { PropTypes, Component } from 'react';
import { connect } from 'react-redux';


class CommentsWrapper extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

CommmentsWrapper.propTypes = {

};

export default connect()(CommentsWrapper);